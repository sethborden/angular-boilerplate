(function() {
    angular.module('app', [
    //uncomment as needed
    //'ngAnimate',
    //'ngRoute',
    ]);
    /* Remove the semi-colon and uncomment the following to set up routing, make
     * sure that ngRoute is declared as a dependency and has been uncommented in
     * index.html
    .config(config);

    config.$inject = ['$routeProvider']; 
    function config($routeProvider) {
        $routeProvider
            .when('/random', {
                templateUrl: 'views/random.template.html',
                controller: 'RandomController',
                controllerAs: 'random'
            })
            .otherwise('/')
    }
    */
})();
