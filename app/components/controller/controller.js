(function() {
    'use strict';
    angular.module('app')
    .controller('RandomController', randomController);

    randomController.$inject = ['service', '$scope', '$log'];
    function randomController(service, $scope, $log) {
        
    }
})();
